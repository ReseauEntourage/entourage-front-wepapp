// import { fromJS } from 'immutable';
// import { makeSelectEntourageMapDomain } from '../selectors';

// const selector = makeSelectEntourageMapDomain();

describe('makeSelectEntourageMapDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
